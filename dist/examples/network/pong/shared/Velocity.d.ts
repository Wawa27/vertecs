import { Component } from "../../../../src";
export default class Velocity extends Component {
    #private;
    constructor();
    get x(): number;
    set x(value: number);
    get y(): number;
    set y(value: number);
}
